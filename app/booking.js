class Booking {
    // Booking
    Booking = [];

    constructor() {
        // EXAMPLE: this.teachID = teachID;
    }

    getFormattedDate() {
        // This function takes "1" from the database and returns it as "Sunday"
    }
}