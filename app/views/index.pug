extends layout 
block content
    head 
    title= "Profile Page"

    body 
    h1 #{heading}
    img(src='/images/vicky.png' width='200px' height='350px')
    div.name Vicky
    div.skills Skills
        select(name="skills_list")
            option(value=1)
            option(value=2) Academic
            option(value=3) Recreation
    div.book Book